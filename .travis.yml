language: java
script:
  - mvn clean verify --file ./simple-api/pom.xml

#connexion à Docker
  - docker login -u $user -p $pwd

  #Build
  - docker build -t $user/devopsproject:database database
  - docker build -t $user/devopsproject:httpd httpd
  - docker build -t $user/devopsproject:simple-api simple-api

  #Publication des images
  - docker push $user/devopsproject:database
  - docker push $user/devopsproject:httpd
  - docker push $user/devopsproject:simple-api

  - mvn clean verify org.jacoco:jacoco-maven-plugin:prepare-agent install
  sonar:sonar -Dsonar.projectKey=devops-2020
  - mvn clean verify org.jacoco:jacoco-maven-plugin:prepare-agent install sonar:sonar -Dsonar.projectKey=Callyie_tpdevops

  addons:
    sonarcloud:
      organization: "callyie-sc"
      token: "$token-sc"



