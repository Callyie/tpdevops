language: java
script:
  - mvn clean verify --file ./simple-api/pom.xml

  - docker login -u $user -p $pwd

  - docker-compose up

  - docker tag tg $user/devopsproject
  - docker push $user/devopsproject:tg

cache:
directories:
  -$HOME/ .m2

services:
  -docker



